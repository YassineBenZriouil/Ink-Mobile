import { RouteConfig } from '@/router/Routes';
import PreAuth from './main';

const routes: RouteConfig[] = [
    {
        id: 'PreAuth',
        name: 'PreAuth',
        component: PreAuth,
    },
];

export default routes;
